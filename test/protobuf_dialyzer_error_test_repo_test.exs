defmodule ProtobufDialyzerErrorTestRepoTest do
  use ExUnit.Case
  doctest ProtobufDialyzerErrorTestRepo

  test "greets the world" do
    assert ProtobufDialyzerErrorTestRepo.hello() == :world
  end
end
